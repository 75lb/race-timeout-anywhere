{
  "name": "race-timeout-anywhere",
  "author": "Lloyd Brookes <75pound@gmail.com>",
  "version": "1.0.0",
  "description": "A Promise.race timeout function which can be used with any module loader",
  "main": "dist/index.js",
  "repository": "https://github.com/75lb/race-timeout-anywhere",
  "keywords": [
    "promise",
    "race",
    "timeout",
    "isomorphic",
    "es6"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "files": [
    "index.mjs",
    "dist/index.js"
  ],
  "scripts": {
    "test": "npm run dist && node test.js && node --experimental-modules test.mjs",
    "dist": "rollup -f umd -n raceTimeout -o dist/index.js index.mjs"
  },
  "devDependencies": {
    "rollup": "^1.21.2",
    "sleep-anywhere": "^1.1.2"
  }
}
